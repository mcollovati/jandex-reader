<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.mcollovati</groupId>
    <artifactId>jandex-reader</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>Jandex Reader</name>
    <description>Command line tool to inspect Jandex indexes in JAR files and Maven artifacts</description>
    <url>https://github.com/mcollovati/jandex-reader</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>Apache-2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>mcollovati</id>
            <name>Marco Collovati</name>
            <email>mcollovati@gmail.com</email>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/mcollovati/jandex-reader</url>
        <connection>scm:git:https://github.com/mcollovati/jandex-reader.git</connection>
    </scm>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.version>3.39.5</quarkus.platform.version>
        <jandex.version>3.6.0</jandex.version>
        <compiler-plugin.version>3.14.0</compiler-plugin.version>
        <surefire-plugin.version>3.5.3</surefire-plugin.version>
        <skipITs>true</skipITs>
        <jreleaser-plugin.version>1.26.0</jreleaser-plugin.version>
        <versions-plugin.version>2.22.0</versions-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-picocli</artifactId>
        </dependency>
        <dependency>
            <groupId>io.smallrye</groupId>
            <artifactId>jandex</artifactId>
            <version>${jandex.version}</version>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <!-- only to check the JSON output in tests -->
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <!-- only @property@ is filtered, so Quarkus ${...} expressions stay untouched -->
                    <useDefaultDelimiters>false</useDefaultDelimiters>
                    <delimiters>
                        <delimiter>@</delimiter>
                    </delimiters>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <systemPropertyVariables>
                        <native.image.path>${project.build.directory}/${project.build.finalName}-runner</native.image.path>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <!-- the release workflow sets the version from the tag: mvn versions:set -DnewVersion=... -->
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
                <version>${versions-plugin.version}</version>
                <configuration>
                    <generateBackupPoms>false</generateBackupPoms>
                </configuration>
            </plugin>
            <plugin>
                <!--
                    Run by the release workflow (.github/workflows/release.yml): mvn jreleaser:full-release
                    after setting the version from its input and downloading the binaries into artifacts/.
                    Name, description, links, license, authors and inception year come from this POM.
                    Reference: https://jreleaser.org/guide/latest/reference/index.html
                -->
                <groupId>org.jreleaser</groupId>
                <artifactId>jreleaser-maven-plugin</artifactId>
                <version>${jreleaser-plugin.version}</version>
                <configuration>
                    <jreleaser>
                        <project>
                            <longDescription>Command line tool that checks whether JAR files, directories or Maven artifacts contain a Jandex index (META-INF/jandex.idx) and lets you browse its content: classes, members, annotations and type hierarchy.</longDescription>
                            <stereotype>CLI</stereotype>
                            <languages>
                                <java>
                                    <version>21</version>
                                </java>
                            </languages>
                        </project>
                        <release>
                            <github>
                                <owner>mcollovati</owner>
                                <name>jandex-reader</name>
                                <!-- JReleaser creates the tag on the released commit; release.yml checks it does not exist yet -->
                                <tagName>{{projectVersion}}</tagName>
                                <releaseName>{{projectVersion}}</releaseName>
                                <overwrite>false</overwrite>
                                <prerelease>
                                    <!-- versions with a suffix, such as 1.1.0-rc1 -->
                                    <pattern>.*-.*</pattern>
                                </prerelease>
                                <changelog>
                                    <formatted>ALWAYS</formatted>
                                    <skipMergeCommits>true</skipMergeCommits>
                                    <contributors>
                                        <enabled>false</enabled>
                                    </contributors>
                                    <hide>
                                        <uncategorized>false</uncategorized>
                                    </hide>
                                </changelog>
                            </github>
                        </release>
                        <checksum>
                            <individual>false</individual>
                        </checksum>
                        <distributions>
                            <jandex-reader>
                                <type>BINARY</type>
                                <executable>
                                    <name>jandex-reader</name>
                                    <windowsExtension>exe</windowsExtension>
                                </executable>
                                <artifacts>
                                    <artifact>
                                        <path>artifacts/jandex-reader-linux-amd64.tar.gz</path>
                                        <platform>linux-x86_64</platform>
                                    </artifact>
                                    <artifact>
                                        <path>artifacts/jandex-reader-linux-arm64.tar.gz</path>
                                        <platform>linux-aarch_64</platform>
                                    </artifact>
                                    <artifact>
                                        <path>artifacts/jandex-reader-macos-amd64.tar.gz</path>
                                        <platform>osx-x86_64</platform>
                                    </artifact>
                                    <artifact>
                                        <path>artifacts/jandex-reader-macos-arm64.tar.gz</path>
                                        <platform>osx-aarch_64</platform>
                                    </artifact>
                                    <artifact>
                                        <path>artifacts/jandex-reader-windows-amd64.zip</path>
                                        <platform>windows-x86_64</platform>
                                    </artifact>
                                </artifacts>
                            </jandex-reader>
                        </distributions>
                        <files>
                            <artifacts>
                                <!-- JVM uber-jar, runs with java -jar on Java 21+ -->
                                <artifact>
                                    <path>artifacts/jandex-reader.jar</path>
                                </artifact>
                            </artifacts>
                        </files>
                    </jreleaser>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <activation>
                <property>
                    <name>native</name>
                </property>
            </activation>
            <properties>
                <skipITs>false</skipITs>
                <quarkus.native.enabled>true</quarkus.native.enabled>
            </properties>
        </profile>
    </profiles>
</project>
